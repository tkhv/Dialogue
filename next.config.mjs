/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "a.ltrbxd.com",
      "image.tmdb.org",
      "via.placeholder.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
